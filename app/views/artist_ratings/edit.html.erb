<!-- Validation failure messages -->

<% if @artist_rating.errors.any? %>
  <% @artist_rating.errors.full_messages.each do |message| %>
    <div class="alert alert-dismissable alert-danger">
      <button type="button" class="close" data-dismiss="alert" aria-hidden="true">&times;</button>
      <%= message %>
    </div>
  <% end %>
<% end %>

<!-- Responsive panel example -->

<div class="row">
  <div class="col-sm-offset-1 col-sm-10 col-md-offset-2 col-md-8">
    <div class="panel panel-default">
      <div class="panel-heading">
        <h3 class="panel-title">Edit Artist Rating #<%= @artist_rating.id %></h3>
      </div>
      <div class="panel-body">
        <%= form_tag artist_rating_url(@artist_rating.id), :method => "patch", :class => "form-horizontal" do %>
          <!-- Label and input for artist_id -->
          <div class="form-group">
            <label for="artist_id" class="col-sm-2 control-label">
              Artist
            </label>
            <div class="col-sm-10">
              <input type="text" id="artist_id" name="artist_id" placeholder="Artist" class="form-control" value="<%= @artist_rating.artist_id %>">
            </div>
          </div>

          <!-- Label and input for user_id -->
          <div class="form-group">
            <label for="user_id" class="col-sm-2 control-label">
              User
            </label>
            <div class="col-sm-10">
              <input type="text" id="user_id" name="user_id" placeholder="User" class="form-control" value="<%= @artist_rating.user_id %>">
            </div>
          </div>

          <div class="form-group">
            <div class="col-sm-offset-2 col-sm-10">
              <input type="submit" value="Update Artist Rating" class="btn btn-warning">
              or
              <%= link_to "Cancel", artist_rating_url(@artist_rating.id) %>
            </div>
          </div>
        <% end %>
      </div>
    </div>
  </div>
</div>


<!-- End responsive panel example -->
